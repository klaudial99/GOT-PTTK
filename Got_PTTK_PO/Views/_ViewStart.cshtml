@{
    @if (User.IsInRole("Admin"))
        Layout = "_Layout_Admin";
    else if (User.IsInRole("Turysta"))
        Layout = "_Layout_Turysta";
    else if (User.IsInRole("Przodownik"))
        Layout = "_Layout_Przodownik";
    else
        Layout = "_Layout";
}
